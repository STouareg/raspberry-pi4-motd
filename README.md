# raspberry-pi4-motd

```
sudo rm -rf /etc/motd
sudo chmod -x /etc/update-motd.d/*
vim ~/.zprofile
```


---

#### some old commands:


added to /etc/profile.d/motd.sh

chmod +x /etc/profile.d/motd.sh
